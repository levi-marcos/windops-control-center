services:
  - type: web
    name: windops-api
    runtime: node
    repo: https://github.com/levi-marcos/windops-api
    plan: free
    buildCommand: npm ci && npx prisma generate && npm run build
    startCommand: npx prisma migrate deploy && npm run start:prod
    envVars:
      - key: NODE_VERSION
        value: 24
      - key: DATABASE_URL
        sync: false